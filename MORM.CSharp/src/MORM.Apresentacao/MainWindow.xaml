<comps:AbstractWindow x:Class="MORM.Apresentacao.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:comps="clr-namespace:MORM.Apresentacao"
        mc:Ignorable="d"
        BorderThickness="0"
        WindowStyle="None"
        ResizeMode="NoResize"
        Padding="0"
        Margin="0"
        Title="MainWindow" 
        MinHeight="250" MinWidth="250"
        ShowInTaskbar="True"
        Height="300" Width="Auto">
    <Grid>
        <!--#region Principal-->
        <DockPanel>
            <ContentControl Content="{Binding TituloSistema}" DockPanel.Dock="Top" Width="Auto"/>
            <ContentControl Content="{Binding MenuLateral}" DockPanel.Dock="Left" Width="Auto"/>
            <ContentControl Content="{Binding Corpo}" />
        </DockPanel>
        <!--#endregion-->
    </Grid>
</comps:AbstractWindow>